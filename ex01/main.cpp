#include "phonebook.hpp"

int	main()
{
	Phonebook phonebook;
	std::string cmnd = nullptr;
	while (1)
	{
		std::cout << "Enter your command: ";
		std::cin >> cmnd;
		if (cmnd.empty())
			break;
		// if ()
		
	}
}